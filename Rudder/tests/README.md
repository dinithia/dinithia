# Tests

Cross-cutting test suites that span multiple deployables or packages.

| Directory | Purpose |
|-----------|---------|
| `e2e/` | End-to-end flows across apps and services |
| `integration/` | Multi-component integration tests |
| `performance/` | Load, latency, and capacity tests |

Unit tests typically live next to the code they cover inside each workspace package.
