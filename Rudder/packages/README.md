# Packages

Reusable libraries shared across apps, services, and agents.

Keep packages focused and versioned within the workspace. Prefer domain-oriented package names when a library encodes business concepts.
