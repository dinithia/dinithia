# Tools

Internal developer tooling.

| Directory | Purpose |
|-----------|---------|
| `scripts/` | One-off and maintenance scripts |
| `generators/` | Code generators and scaffolding |

Notable scripts:

- `scripts/validate_team_directory.py` — ensure `team/` users, roles, and membership stay consistent.
