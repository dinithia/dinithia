# Apps

Deployable user-facing applications (web, mobile, desktop clients).

Each subdirectory is an independently buildable and shippable app that may depend on packages from `packages/` and contracts from `contracts/`.
